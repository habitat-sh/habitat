steps:
  - label: "Deploy the website :pen:"
    command:
      - .expeditor/scripts/verify/website-deploy.sh
    expeditor:
      executor:
        docker:
    timeout_in_minutes: 10
    retry:
      automatic:
        limit: 1
