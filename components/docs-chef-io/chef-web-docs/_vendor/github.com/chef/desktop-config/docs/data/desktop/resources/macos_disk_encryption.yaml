---
resource_reference: true
properties_shortcode:
resources_common_guards: true
resources_common_notification: true
resources_common_properties: true
title: macos_disk_encryption resource
resource: macos_disk_encryption
aliases:
- "/resource_macos_disk_encryption.html"
menu:
  desktop:
    title: macos_disk_encryption
    identifier: desktop/resources/macos_disk_encryption macos_disk_encryption
    parent: desktop/resources
resource_description_list:
- markdown: Use the **macos_disk_encryption** resource to enforce FileVault encryption
    on macOS systems.
resource_new_in: '1.0'
syntax_full_code_block: |-
  macos_disk_encryption 'name' do
    action      Symbol # defaults to :enable if not specified
  end
syntax_properties_list:
syntax_full_properties_list:
- "`macos_disk_encryption` is the resource."
- "`name` is the name given to the resource block."
- "`action` identifies which steps Chef Infra Client will take to bring the node into
  the desired state."
actions_list:
  :enable:
    markdown: "Turns on FileVault."
  :nothing:
    shortcode: resources_common_actions_nothing.md
properties_list: []
examples:
