---
resource_reference: true
properties_shortcode:
resources_common_guards: true
resources_common_notification: true
resources_common_properties: true
title: windows_admin_control resource
resource: windows_admin_control
aliases:
- "/resource_windows_admin_control.html"
menu:
  desktop:
    title: windows_admin_control
    identifier: desktop/resources/windows_admin_control windows_admin_control
    parent: desktop/resources
resource_description_list:
- markdown: Use the **windows_admin_control** resource to enforce Admin level access
    for system-wide changes.
resource_new_in: '1.0'
syntax_full_code_block: |-
  windows_admin_control 'name' do
    action      Symbol # defaults to :enable if not specified
  end
syntax_properties_list:
syntax_full_properties_list:
- "`windows_admin_control` is the resource."
- "`name` is the name given to the resource block."
- "`action` identifies which steps Chef Infra Client will take to bring the node into
  the desired state."
actions_list:
  :disable:
    markdown:
  :enable:
    markdown:
  :nothing:
    shortcode: resources_common_actions_nothing.md
properties_list: []
examples:
