# PostgreSQL configuration file
# This file was automatically generated by Bldr
# Please refer to the PostgreSQL documentation for details on
# configuration settings.

# Connections and Authentication
listen_addresses = '{{listen_addresses}}'
port = {{port}}
data_directory = '{{data_directory}}'
unix_socket_directories = '{{unix_socket_directories}}'
external_pid_file = '{{external_pid_file}}'
hba_file = '{{hba_file}}'
#ident_file = '{{ident_file}}'
ssl = {{ssl}}
ssl_cert_file = '{{ssl_cert_file}}'
ssl_key_file = '{{ssl_key_file}}'

# Resource Usage
max_connections = {{max_connections}}
shared_buffers = '{{shared_buffers}}'
work_mem = '{{work_mem}}'
effective_cache_size = '{{effective_cache_size}}' # TODO: can this be auto-tuned based on physical RAM available?
max_wal_size = '{{max_wal_size}}'
min_wal_size = '{{min_wal_size}}'
checkpoint_completion_target = {{checkpoint_completion_target}}
checkpoint_timeout = '{{checkpoint_timeout}}'
checkpoint_warning = '{{checkpoint_warning}}'
dynamic_shared_memory_type = '{{dynamic_shared_memory_type}}'

# Error Reporting and Logging
log_destination = '{{log_destination}}'
log_line_prefix = '{{log_line_prefix}}'
logging_collector = {{logging_collector}}
timezone = '{{timezone}}'
log_min_duration_statement = {{log_min_duration_statement}}

# Client Connection Defaults
datestyle = '{{datestyle}}'
default_text_search_config = '{{default_text_search_config}}'

# Replication settings
wal_level = '{{wal_level}}'
hot_standby = '{{hot_standby}}'
shared_preload_libraries = '{{shared_preload_libraries}}'
archive_command = '{{archive_command}}'
archive_mode = {{archive_mode}}
max_replication_slots = {{max_replication_slots}}
max_wal_senders = {{max_wal_senders}}
max_worker_processes = {{max_worker_processes}}
# not sure this will work with bldr's path being different per package
#pgpool.pg_ctl = {{pgpool_pg_ctl}}
