data_directory = '{{cfg.data_directory}}'
hba_file = '{{cfg.hba_file}}'
ident_file = '{{cfg.ident_file}}'
external_pid_file = '{{cfg.external_pid_file}}'
listen_addresses = '{{cfg.listen_addresses}}'
port = {{cfg.port}}
max_connections = {{cfg.max_connections}}
superuser_reserved_connections = {{cfg.superuser_reserved_connections}}
unix_socket_directories = '{{cfg.unix_socket_directories}}'
unix_socket_group = '{{cfg.unix_socket_group}}'
unix_socket_permissions = {{cfg.unix_socket_permissions}}
bonjour = {{cfg.bonjour}}
bonjour_name = '{{cfg.bonjour_name}}'
authentication_timeout = '{{cfg.authentication_timeout}}'
ssl = {{cfg.ssl}}
ssl_ciphers = '{{cfg.ssl_ciphers}}'
ssl_prefer_server_ciphers = {{cfg.ssl_prefer_server_ciphers}}
ssl_ecdh_curve = '{{cfg.ssl_ecdh_curve}}'
ssl_cert_file = '{{cfg.ssl_cert_file}}'
ssl_key_file = '{{cfg.ssl_key_file}}'
ssl_ca_file = '{{cfg.ssl_ca_file}}'
ssl_crl_file = '{{cfg.ssl_crl_file}}'
password_encryption = {{cfg.password_encryption}}
db_user_namespace = {{cfg.db_user_namespace}}
row_security = {{cfg.row_security}}
krb_server_keyfile = '{{cfg.krb_server_keyfile}}'
krb_caseins_users = {{cfg.krb_caseins_users}}
tcp_keepalives_idle = {{cfg.tcp_keepalives_idle}}
tcp_keepalives_interval = {{cfg.tcp_keepalives_interval}}
tcp_keepalives_count = {{cfg.tcp_keepalives_count}}
shared_buffers = '{{cfg.shared_buffers}}'
huge_pages = '{{cfg.huge_pages}}'
temp_buffers = '{{cfg.temp_buffers}}'
max_prepared_transactions = {{cfg.max_prepared_transactions}}
work_mem = '{{cfg.work_mem}}'
maintenance_work_mem = '{{cfg.maintenance_work_mem}}'
autovacuum_work_mem = {{cfg.autovacuum_work_mem}}
max_stack_depth = '{{cfg.max_stack_depth}}'
dynamic_shared_memory_type = '{{cfg.dynamic_shared_memory_type}}'
temp_file_limit = {{cfg.temp_file_limit}}
max_files_per_process = {{cfg.max_files_per_process}}
shared_preload_libraries = '{{cfg.shared_preload_libraries}}'
vacuum_cost_delay = {{cfg.vacuum_cost_delay}}
vacuum_cost_page_hit = {{cfg.vacuum_cost_page_hit}}
vacuum_cost_page_miss = {{cfg.vacuum_cost_page_miss}}
vacuum_cost_page_dirty = {{cfg.vacuum_cost_page_dirty}}
vacuum_cost_limit = {{cfg.vacuum_cost_limit}}
bgwriter_delay = '{{cfg.bgwriter_delay}}'
bgwriter_lru_maxpages = {{cfg.bgwriter_lru_maxpages}}
bgwriter_lru_multiplier = {{cfg.bgwriter_lru_multiplier}}
effective_io_concurrency = {{cfg.effective_io_concurrency}}
max_worker_processes = {{cfg.max_worker_processes}}
wal_level = '{{cfg.wal_level}}'
fsync = {{cfg.fsync}}
synchronous_commit = {{cfg.synchronous_commit}}
wal_sync_method = '{{cfg.wal_sync_method}}'
full_page_writes = {{cfg.full_page_writes}}
wal_compression = {{cfg.wal_compression}}
wal_log_hints = {{cfg.wal_log_hints}}
wal_buffers = {{cfg.wal_buffers}}
wal_writer_delay = '{{cfg.wal_writer_delay}}'
commit_delay = {{cfg.commit_delay}}
commit_siblings = {{cfg.commit_siblings}}
checkpoint_timeout = '{{cfg.checkpoint_timeout}}'
max_wal_size = '{{cfg.max_wal_size}}'
min_wal_size = '{{cfg.min_wal_size}}'
checkpoint_completion_target = {{cfg.checkpoint_completion_target}}
checkpoint_warning = '{{cfg.checkpoint_warning}}'
archive_mode = {{cfg.archive_mode}}
archive_command = '{{cfg.archive_command}}'
archive_timeout = {{cfg.archive_timeout}}
max_wal_senders = {{cfg.max_wal_senders}}
wal_keep_segments = {{cfg.wal_keep_segments}}
wal_sender_timeout = '{{cfg.wal_sender_timeout}}'
max_replication_slots = {{cfg.max_replication_slots}}
track_commit_timestamp = {{cfg.track_commit_timestamp}}
synchronous_standby_names = '{{cfg.synchronous_standby_names}}'
vacuum_defer_cleanup_age = {{cfg.vacuum_defer_cleanup_age}}
hot_standby = {{cfg.hot_standby}}
max_standby_archive_delay = '{{cfg.max_standby_archive_delay}}'
max_standby_streaming_delay = '{{cfg.max_standby_streaming_delay}}'
wal_receiver_status_interval = '{{cfg.wal_receiver_status_interval}}'
hot_standby_feedback = {{cfg.hot_standby_feedback}}
wal_receiver_timeout = '{{cfg.wal_receiver_timeout}}'
wal_retrieve_retry_interval = '{{cfg.wal_retrieve_retry_interval}}'
enable_bitmapscan = {{cfg.enable_bitmapscan}}
enable_hashagg = {{cfg.enable_hashagg}}
enable_hashjoin = {{cfg.enable_hashjoin}}
enable_indexscan = {{cfg.enable_indexscan}}
enable_indexonlyscan = {{cfg.enable_indexonlyscan}}
enable_material = {{cfg.enable_material}}
enable_mergejoin = {{cfg.enable_mergejoin}}
enable_nestloop = {{cfg.enable_nestloop}}
enable_seqscan = {{cfg.enable_seqscan}}
enable_sort = {{cfg.enable_sort}}
enable_tidscan = {{cfg.enable_tidscan}}
seq_page_cost = {{cfg.seq_page_cost}}
random_page_cost = {{cfg.random_page_cost}}
cpu_tuple_cost = {{cfg.cpu_tuple_cost}}
cpu_index_tuple_cost = {{cfg.cpu_index_tuple_cost}}
cpu_operator_cost = {{cfg.cpu_operator_cost}}
effective_cache_size = '{{cfg.effective_cache_size}}'
geqo = {{cfg.geqo}}
geqo_threshold = {{cfg.geqo_threshold}}
geqo_effort = {{cfg.geqo_effort}}
geqo_pool_size = {{cfg.geqo_pool_size}}
geqo_generations = {{cfg.geqo_generations}}
geqo_selection_bias = {{cfg.geqo_selection_bias}}
geqo_seed = {{cfg.geqo_seed}}
default_statistics_target = {{cfg.default_statistics_target}}
constraint_exclusion = '{{cfg.constraint_exclusion}}'
cursor_tuple_fraction = {{cfg.cursor_tuple_fraction}}
from_collapse_limit = {{cfg.from_collapse_limit}}
join_collapse_limit = {{cfg.join_collapse_limit}}
log_destination = '{{cfg.log_destination}}'
logging_collector = {{cfg.logging_collector}}
log_directory = '{{cfg.log_directory}}'
log_filename = '{{cfg.log_filename}}'
log_file_mode = {{cfg.log_file_mode}}
log_truncate_on_rotation = {{cfg.log_truncate_on_rotation}}
log_rotation_age = '{{cfg.log_rotation_age}}'
log_rotation_size = '{{cfg.log_rotation_size}}'
syslog_facility = '{{cfg.syslog_facility}}'
syslog_ident = '{{cfg.syslog_ident}}'
event_source = '{{cfg.event_source}}'
client_min_messages = '{{cfg.client_min_messages}}'
log_min_messages = '{{cfg.log_min_messages}}'
log_min_error_statement = '{{cfg.log_min_error_statement}}'
log_min_duration_statement = {{cfg.log_min_duration_statement}}
debug_print_parse = {{cfg.debug_print_parse}}
debug_print_rewritten = {{cfg.debug_print_rewritten}}
debug_print_plan = {{cfg.debug_print_plan}}
debug_pretty_print = {{cfg.debug_pretty_print}}
log_checkpoints = {{cfg.log_checkpoints}}
log_connections = {{cfg.log_connections}}
log_disconnections = {{cfg.log_disconnections}}
log_duration = {{cfg.log_duration}}
log_error_verbosity = '{{cfg.log_error_verbosity}}'
log_hostname = {{cfg.log_hostname}}
log_line_prefix = '{{cfg.log_line_prefix}}'
log_lock_waits = {{cfg.log_lock_waits}}
log_statement = '{{cfg.log_statement}}'
log_replication_commands = {{cfg.log_replication_commands}}
log_temp_files = {{cfg.log_temp_files}}
log_timezone = '{{cfg.log_timezone}}'
cluster_name = '{{cfg.cluster_name}}'
update_process_title = {{cfg.update_process_title}}
track_activities = {{cfg.track_activities}}
track_counts = {{cfg.track_counts}}
track_io_timing = {{cfg.track_io_timing}}
track_functions = '{{cfg.track_functions}}'
track_activity_query_size = {{cfg.track_activity_query_size}}
stats_temp_directory = '{{cfg.stats_temp_directory}}'
log_parser_stats = {{cfg.log_parser_stats}}
log_planner_stats = {{cfg.log_planner_stats}}
log_executor_stats = {{cfg.log_executor_stats}}
log_statement_stats = {{cfg.log_statement_stats}}
autovacuum = {{cfg.autovacuum}}
log_autovacuum_min_duration = {{cfg.log_autovacuum_min_duration}}
autovacuum_max_workers = {{cfg.autovacuum_max_workers}}
autovacuum_naptime = '{{cfg.autovacuum_naptime}}'
autovacuum_vacuum_threshold = {{cfg.autovacuum_vacuum_threshold}}
autovacuum_analyze_threshold = {{cfg.autovacuum_analyze_threshold}}
autovacuum_vacuum_scale_factor = {{cfg.autovacuum_vacuum_scale_factor}}
autovacuum_analyze_scale_factor = {{cfg.autovacuum_analyze_scale_factor}}
autovacuum_freeze_max_age = {{cfg.autovacuum_freeze_max_age}}
autovacuum_multixact_freeze_max_age = {{cfg.autovacuum_multixact_freeze_max_age}}
autovacuum_vacuum_cost_delay = '{{cfg.autovacuum_vacuum_cost_delay}}'
autovacuum_vacuum_cost_limit = {{cfg.autovacuum_vacuum_cost_limit}}
search_path = '{{cfg.search_path}}'
default_tablespace = '{{cfg.default_tablespace}}'
temp_tablespaces = '{{cfg.temp_tablespaces}}'
check_function_bodies = {{cfg.check_function_bodies}}
default_transaction_isolation = '{{cfg.default_transaction_isolation}}'
default_transaction_read_only = {{cfg.default_transaction_read_only}}
default_transaction_deferrable = {{cfg.default_transaction_deferrable}}
session_replication_role = '{{cfg.session_replication_role}}'
statement_timeout = {{cfg.statement_timeout}}
lock_timeout = {{cfg.lock_timeout}}
vacuum_freeze_min_age = {{cfg.vacuum_freeze_min_age}}
vacuum_freeze_table_age = {{cfg.vacuum_freeze_table_age}}
vacuum_multixact_freeze_min_age = {{cfg.vacuum_multixact_freeze_min_age}}
vacuum_multixact_freeze_table_age = {{cfg.vacuum_multixact_freeze_table_age}}
bytea_output = '{{cfg.bytea_output}}'
xmlbinary = '{{cfg.xmlbinary}}'
xmloption = '{{cfg.xmloption}}'
gin_fuzzy_search_limit = {{cfg.gin_fuzzy_search_limit}}
gin_pending_list_limit = '{{cfg.gin_pending_list_limit}}'
datestyle = '{{cfg.datestyle}}'
intervalstyle = '{{cfg.intervalstyle}}'
timezone = '{{cfg.timezone}}'
timezone_abbreviations = '{{cfg.timezone_abbreviations}}'
extra_float_digits = {{cfg.extra_float_digits}}
client_encoding = '{{cfg.client_encoding}}'
lc_messages = '{{cfg.lc_messages}}'
lc_monetary = '{{cfg.lc_monetary}}'
lc_numeric = '{{cfg.lc_numeric}}'
lc_time = '{{cfg.lc_time}}'
default_text_search_config = '{{cfg.default_text_search_config}}'
dynamic_library_path = '{{cfg.dynamic_library_path}}'
local_preload_libraries = '{{cfg.local_preload_libraries}}'
session_preload_libraries = '{{cfg.session_preload_libraries}}'
deadlock_timeout = '{{cfg.deadlock_timeout}}'
max_locks_per_transaction = {{cfg.max_locks_per_transaction}}
max_pred_locks_per_transaction = {{cfg.max_pred_locks_per_transaction}}
array_nulls = {{cfg.array_nulls}}
backslash_quote = '{{cfg.backslash_quote}}'
default_with_oids = {{cfg.default_with_oids}}
escape_string_warning = {{cfg.escape_string_warning}}
lo_compat_privileges = {{cfg.lo_compat_privileges}}
operator_precedence_warning = {{cfg.operator_precedence_warning}}
quote_all_identifiers = {{cfg.quote_all_identifiers}}
sql_inheritance = {{cfg.sql_inheritance}}
standard_conforming_strings = {{cfg.standard_conforming_strings}}
synchronize_seqscans = {{cfg.synchronize_seqscans}}
transform_null_equals = {{cfg.transform_null_equals}}
exit_on_error = {{cfg.exit_on_error}}
restart_after_crash = {{cfg.restart_after_crash}}
include_dir = '{{cfg.include_dir}}'
