###################################################
# This file was generated by Habitat.
#
# For tunable settings see `default.toml` in the core/openvpn package
###################################################
;local {{cfg.local}}
port {{cfg.port}}
proto {{cfg.proto}}
dev {{cfg.dev}}
{{~#if cfg.dev-node}}
dev-node {{cfg.dev-node}}
{{~/if}}
ca {{cfg.ca}}
cert {{cfg.cert}}
key {{cfg.key}}
dh {{cfg.dh}}
{{~#if cfg.server}}
server {{cfg.server}}
{{~/if}}
ifconfig-pool-persist {{cfg.ifconfig-pool-persist}}
{{~#if cfg.server-bridge}}
server-bridge {{cfg.server-bridge}}
{{~/if}}
{{~#each cfg.push}}
push "{{this}}"
{{~/each}}
{{~#if cfg.learn-address}}
learn-address {{cfg.learn-address}}
{{~/if}}
{{~#if cfg.client-to-client}}
client-to-client
{{~/if}}
{{~#if cfg.duplicate-cn}}
duplicate-cn
{{~/if}}
keepalive {{cfg.keepalive}}
{{~#if cfg.tls-auth}}
tls-auth {{cfg.tls-auth}}
{{~/if}}
cipher {{cfg.cipher}}
{{~#if cfg.comp-lzo}}
comp-lzo
{{~/if}}
max-clients {{cfg.max-clients}}
user {{cfg.user}}
group {{cfg.group}}
{{~#if cfg.persist-key}}
persist-key
{{~/if}}
{{~#if cfg.persist-tun}}
persist-tun
{{~/if}}
status {{cfg.status}}
{{~#if cfg.log}}
log {{cfg.log}}
{{~/if}}
{{~#if cfg.log-append}}
log-append {{cfg.log-append}}
{{~/if}}
verb {{cfg.verb}}
mute {{cfg.mute}}
